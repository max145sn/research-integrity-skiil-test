# Trial packages

This folder contains separate synthetic fixtures for trial runs.

## Controlled synthetic claim-audit package

- `controlled_synthetic_claim_audit_package.zip` — original archive
- `controlled_synthetic_claim_audit_package/` — extracted package contents

## Compact tabular benchmark package

- `compact_tabular_benchmark_package.zip` — original archive
- `compact_tabular_benchmark_package/` — extracted package contents
